{
  "title": "quote",
  "description": "describes properties required to create a user",
  "type": "object",
  "properties": {
    "digital_currency": {
      "type": "string",
      "description": "digital currency of interest"
    },
    "fiat_currency": {
      "type": "string",
      "description": "fiat currency of interest"
    },
    "requested_currency": {
      "type": "string",
      "description": "the currency received from the transaction"
    },
    "requested_amount": {
      "type": "number",
      "description": "how much of the requested_currency is wanted"
    },
    "client_id": {
      "type": "string",
      "description": "user id generated by the client app"
    }
  },
  "required": [
    "digital_currency",
    "fiat_currency",
    "requested_currency",
    "requested_amount",
    "client_id"
  ]
}

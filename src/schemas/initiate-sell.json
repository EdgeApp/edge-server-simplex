{
  "title": "initiate-sell",
  "description": "",
  "type": "object",
  "properties": {
    "quote": {
      "type": "object",
      "description": "the quote from the getQuote endpoint"
    },
    "return_url": {
      "type": "string",
      "description": "a uri to redirect back to the app after simplex's intake"
    },
    "refund_crypto_address": {
      "type": "string",
      "description": "the address to refund the crypto to if something goes wrong"
    },
    "user_id": {
      "type": "string",
      "description": "the user id"
    }
  },
  "required": [
    "quote",
    "return_url",
    "refund_crypto_address",
    "user_id"
  ]
}

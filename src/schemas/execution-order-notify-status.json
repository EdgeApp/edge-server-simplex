{
  "title": "execution-order-notify-status",
  "description": "",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "an opaque string to identify this execution order"
    },
    "status": {
      "enum": [
        "completed", "failed", "cancelled"
      ],
      "description": "the status, either pending, failed or completed"
    },
    "cryptoAmountSent": {
      "type": "string",
      "description": "the amount of crypto sent"
    },
    "txnHash": {
      "type": "string",
      "description": "blockchain transaction hash"
    }
  },
  "required": [
    "id",
    "status"
  ]
}
